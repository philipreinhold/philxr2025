export interface DrawingStructureProps {
    initialPosition: [number, number, number];
    initialDelay?: number;
    colorScheme?: string;
  }
  
  export interface ExhibitionRoomProps {
    width?: number;
    height?: number;
    depth?: number;
  }